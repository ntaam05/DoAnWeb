@model IEnumerable<WebDoAn.Models.RoomPost>

@{
    ViewData["Title"] = "Quản lý bài tìm người ở ghép";
}

<div class="container py-4">
    <h2 class="mb-4">Quản lý bài tìm người ở ghép</h2>

    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success">@TempData["Message"]</div>
    }

    @if (TempData["Error"] != null)
    {
        <div class="alert alert-danger">@TempData["Error"]</div>
    }

    <div class="card shadow-sm border-0">
        <div class="card-body">
            <table class="table table-bordered table-hover align-middle">
                <thead class="table-light">
                    <tr>
                        <th>ID</th>
                        <th>Tiêu đề</th>
                        <th>Giá mong muốn</th>
                        <th>Người đăng</th>
                        <th>Mã tham gia</th>
                        <th width="220">Hành động</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Title</td>
                            <td>@item.Price.ToString("N0") VNĐ</td>
                            <td>@item.OwnerId</td>
                            <td>@item.JoinCode</td>
                            <td>
                                <a asp-action="ViewFindDetails" asp-route-postId="@item.Id" class="btn btn-sm btn-primary">
                                    Xem
                                </a>

                                <form asp-action="DeleteFindPost" method="post" class="d-inline">
                                    <input type="hidden" name="postId" value="@item.Id" />
                                    <button type="submit" class="btn btn-sm btn-danger"
                                            onclick="return confirm('Bạn có chắc muốn xóa bài đăng này?')">
                                        Xóa
                                    </button>
                                </form>
                            </td>
                        </tr>
                    }

                    @if (!Model.Any())
                    {
                        <tr>
                            <td colspan="6" class="text-center text-muted">Chưa có bài tìm người nào.</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>